; Pin this repo to the public npm registry. Without this, a machine configured
; against a private mirror bakes unreachable internal URLs into package-lock.json
; and Vercel's install fails with ENOTFOUND.
registry=https://registry.npmjs.org/
