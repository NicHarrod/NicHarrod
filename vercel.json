{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    { "src": "/avas-room/?", "dest": "/avas-room.html" },
    { "handle": "filesystem" }
  ]
}
